terraform {
  required_version = "~>1.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.53.0"
    }
  }

  backend "s3" {
    region = "{{ region }}"
    bucket = "{{ s3_bucket }}"
    key    = "{{ key }}"
  }
}
